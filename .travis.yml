language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-deslight' ]; then cd .. && eval "mv $currentfolder generator-deslight" && cd generator-deslight; fi

